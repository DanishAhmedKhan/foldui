export const NodeSpec = {
    fields: {
        type: 'string',
        props: 'object',
        style: 'object',
        responsive: 'object',
        children: 'array',
    },

    defaults: {
        props: {},
        style: {},
        responsive: {},
    },
}
